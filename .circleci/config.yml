version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: pwd
  test:
    docker:
      - image: circleci/python
workflows:
  version: 2.1
  build_and_test:
    jobs:
      - build
      - test
